class BallDetector:
    """Task 3: Moving ball detection stub — teammate fills this in"""
    def process(self, frame):
        # Returns: (blocking: bool, ttc: float, annotated_frame)
        return False, 0.0, frame